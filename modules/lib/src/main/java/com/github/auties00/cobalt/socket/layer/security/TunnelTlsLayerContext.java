package com.github.auties00.cobalt.socket.layer.security;

/**
 * A TLS layer context for tunnel-level encryption (client to proxy).
 */
public final class TunnelTlsLayerContext extends TlsSocketClientLayerContext {
}
